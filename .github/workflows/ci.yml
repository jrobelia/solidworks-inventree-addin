name: CI

on:
  push:
    branches: [ "**" ]

jobs:
  build-and-test:
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Test
        run: dotnet test --configuration Release --verbosity normal
