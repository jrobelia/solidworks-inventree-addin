# JsonFileConfigProvider removed in favour of encrypted settings

The encrypted settings panel replaces the JSON file approach. Keeping both alive would mean two code paths and user confusion. Clean cut.
