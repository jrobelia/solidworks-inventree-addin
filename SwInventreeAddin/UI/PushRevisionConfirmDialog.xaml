<Window
    x:Class="SwInventreeAddin.UI.PushRevisionConfirmDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Confirm Push"
    Width="420"
    SizeToContent="Height"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    FontFamily="Segoe UI"
    FontSize="12">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="DesignTokens.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <StackPanel>

        <!-- Blue header stripe -->
        <Border Height="4" Background="{StaticResource BrushAccentBlue}" />

        <StackPanel Margin="20,16,20,20">

            <TextBlock TextWrapping="Wrap" Margin="0,0,0,16"
                       Foreground="{StaticResource BrushForeground}">
                Push the SolidWorks revision to InvenTree? This will overwrite
                the revision currently stored in InvenTree.
            </TextBlock>

            <CheckBox x:Name="IncludeImageCheckBox"
                      Content="Also push image to InvenTree"
                      IsChecked="True"
                      Margin="0,0,0,20"
                      FontSize="{StaticResource FontSizeBody}" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="Push"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Margin="0"
                        Click="Push_Click" />

                <Button Grid.Column="2" Content="Cancel"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Margin="0"
                        Click="Cancel_Click" />
            </Grid>

        </StackPanel>
    </StackPanel>
</Window>
